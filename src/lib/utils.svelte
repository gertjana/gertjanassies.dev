<script lang="ts">
    // import type { postType } from './utils.types.ts';

    // type postType = [string, () => Promise<{ metadata: any }>];

    // export async function postsForTag(posts: [string, () => Promise<{ metadata: any }>][], tag: string) {
    //     await Promise.all(
    //     posts.map(async ([path, page]) => {
    //         const { metadata } = await page();
    //         const postPath = path.replace(/^\/blogs\/(.*)\.md$/, '$1');
    //         return {
    //             meta: metadata,
    //             path: postPath
    //         };
    //     })
    // );

    // }
</script>